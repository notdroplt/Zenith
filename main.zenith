true = 0 == 0;

max x y = y < x ? x : y;

res = max 3 45;

module >> "@echo" {
    message = 123;
}

module >> "@importList" {
    load = { "std" } ; 
}

module >> "test" {
    true = true;
}

module << "test" {
    false = true;
}